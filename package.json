{
  "name": "playwright-perf-regression",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright suite asserting behavior and interaction speed in the same test, with traces for debug.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "license": "MIT"
}
